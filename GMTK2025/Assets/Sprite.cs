using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Sprite : MonoBehaviour
{
    public enum Sprites
    {
        OpenHand,
        PointingHand,
        ClosedHand,
        HoldingCloth,
        HoldingKnife,
        HoldingWheel
    }

    public Sprites thisSprite;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
